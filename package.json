{
  "name": "work-trial-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Shared dashboard for tracking work-trial candidate scheduling and onboarding tasks.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --env-file=.env server.js",
    "test": "node --test test/*.test.js",
    "test:clock": "sh test/helpers/clock-sweep.sh"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.12.0",
    "playwright": "1.63.0"
  }
}
